rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // OPEN ACCESS PER USER REQUEST TO REMOVE LOGIN
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
